# Docs

- `apple-tv-mcp-spec/` — project spec, ADRs, and checklist (tracked).
- `assets/` — README images (tracked).
- `references/` — pinned upstream documentation for local agent work. It is
  gitignored and not published. Keep a local copy if you develop against this
  repo; GitHub clones will not include it.
